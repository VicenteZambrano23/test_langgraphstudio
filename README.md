# test_langgraphstudio
Langgraph new ui investigation.
To set up the UI, do the following steps:
1. Set .env following .env.sample.
2. Run the following commands:
    - pip install rav
    - rav run windows_setup (or linux_setup)
